# Extension (existing React/Vite frontend)

Copy or link the existing ONPU AI K2 Studio Chrome extension here.

Enhance with:
- **Mode toggle**: MusicGen (short clips) vs YuE (long-form)
- When YuE selected: genre tags, lyrics (optional), ref audio upload, segment count
- Same Generate / Playback / Analyze DNA flow for both engines

API base URL: configure via env or extension options (default `http://localhost:5000`).
